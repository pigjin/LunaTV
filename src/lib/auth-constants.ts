/**
 * 认证相关常量配置
 */

// Access Token 过期时间
export const ACCESS_TOKEN_EXPIRES_IN = '1h';
export const ACCESS_TOKEN_EXPIRES_IN_SECONDS = 60 * 60; // 1小时

// Refresh Token 过期时间
export const REFRESH_TOKEN_EXPIRES_IN = '30d';
export const REFRESH_TOKEN_EXPIRES_IN_SECONDS = 30 * 24 * 60 * 60; // 30天
